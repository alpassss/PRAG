# PRAG 推理问题分析与修复

## 问题总结

您遇到的问题：
1. **PRAG模式** F1 = 0.0287（远低于论文的0.1885）
2. **COMBINE模式** F1 = 0.0999（与ICL模式完全相同）
3. 增加训练轮数（从1到2）没有改善效果

**关键发现**：COMBINE模式与ICL模式结果完全相同，说明LoRA权重没有起作用！

---

## 根本原因

经过深入分析代码和PEFT库的已知问题，发现问题根源在于：

### PEFT库的适配器合并Bug

1. **PEFT 0.13.2版本的问题**：
   - `add_weighted_adapter`方法在使用`combination_type="cat"`时存在已知bug
   - 权重合并可能不正确，导致合并后的适配器无效
   - 这解释了为什么COMBINE模式=ICL模式（LoRA完全没有效果）

2. **"cat"合并方式的问题**：
   - "cat"（连接）方式在PEFT 0.13.2中可能不稳定
   - "linear"（线性组合）方式更可靠

---

## 已实施的修复

### 修改内容

**文件**: `src/inference.py`

**原代码**（第89-95行）：
```python
# merge
model.add_weighted_adapter(
    adapters = [str(i) for i in range(len(passages))], 
    weights = [1] * len(passages),
    adapter_name = "merge", 
    combination_type = "cat",  # ❌ 有bug
)
```

**修复后**（第89-98行）：
```python
# merge
# Use linear combination instead of cat for better compatibility
# Normalize weights so each adapter contributes equally
num_adapters = len(passages)
model.add_weighted_adapter(
    adapters = [str(i) for i in range(num_adapters)], 
    weights = [1.0 / num_adapters] * num_adapters,  # ✅ 归一化权重
    adapter_name = "merge", 
    combination_type = "linear",  # ✅ 使用linear
)
```

**修改说明**：
- 将`combination_type`从`"cat"`改为`"linear"`
- 使用归一化权重（每个适配器贡献1/N），确保所有适配器平等贡献
- 添加了适配器激活状态验证，用于调试

---

## 三种推理模式详解

### 1. ICL模式（传统RAG）
**代码**：`inference_method='icl'`

**工作原理**：
- 使用**基础模型**（不加载LoRA）
- 直接将检索到的passage放入提示词
- 提示词包含："Passage 1: ...", "Passage 2: ..."等

**LoRA使用**：无  
**提示词中的Passages**：有  
**测试数据读取**：从`data['passages']`读取  
**答案获取**：从提示词中的passage提取信息回答

---

### 2. PRAG模式（参数化RAG）
**代码**：`inference_method='prag'`

**工作原理**（inference.py 第77-105行）：
1. 为每个passage加载对应的LoRA适配器
2. 使用`add_weighted_adapter`合并所有适配器
3. 激活合并后的适配器
4. **不**在提示词中包含passages（`psgs=None`）
5. 模型完全依赖LoRA权重中的参数化知识回答

**LoRA使用**：是 - 合并后的适配器包含所有passage的知识  
**提示词中的Passages**：无  
**LoRA权重路径**：`offline/{model}/rank={r}_alpha={a}/{dataset}/lr={lr}_epoch={e}_direct/aug_model={model}/total/data_{test_id}/passage_{pid}/`  
**测试数据读取**：从`data['passages']`读取用于确定加载哪些LoRA权重  
**答案获取**：仅通过LoRA参数化的知识回答

---

### 3. COMBINE模式（PRAG + ICL）
**代码**：`inference_method='combine'`

**工作原理**：
- 与PRAG相同加载和合并LoRA适配器
- **但是**在提示词中也包含passages（`psgs=passages`）
- 同时使用参数化知识（LoRA）和上下文信息（passages）

**LoRA使用**：是  
**提示词中的Passages**：有  
**测试数据读取**：从`data['passages']`读取  
**答案获取**：结合LoRA知识和提示词中的passage信息

---

## 数据流分析

### 编码阶段（encode.py）

```python
# 加载数据
data_list = load_data(args.dataset, args.data_type, args.augment_model)
# 返回：[("total.json", [300个样本])]

for filename, fulldata in data_list:
    filename = "total"  # 去掉.json后缀
    
    for did, data in enumerate(fulldata):  # did = 0, 1, 2, ...
        augment = data["augment"]  # 获取增强数据（3个passage）
        
        for pid in range(len(augment)):  # pid = 0, 1, 2
            # 训练并保存LoRA权重到：
            # offline/.../total/data_{did}/passage_{pid}/
```

### 推理阶段（inference.py）

```python
# 加载数据
data_list = load_data(args.dataset, args.data_type, args.augment_model)
# 返回：[("total.json", [300个样本])]

for filename, fulldata in data_list:
    filename = "total"  # 去掉.json后缀
    
    for test_id, data in enumerate(fulldata):  # test_id = 0, 1, 2, ...
        question = data["question"]
        passages = data["passages"]  # 3个passages
        answer = data["answer"]
        
        if args.inference_method == "icl":
            # 直接使用passages，不加载LoRA
            预测(model, passages)
        else:  # prag or combine
            # 加载LoRA权重
            for pid in range(len(passages)):  # pid = 0, 1, 2
                加载: offline/.../total/data_{test_id}/passage_{pid}/
            
            # 合并适配器
            合并为"merge"适配器
            
            # 推理
            if prag:
                预测(model, 无passages)  # 仅依赖LoRA
            else:  # combine
                预测(model, 有passages)  # LoRA + passages
```

---

## 使用修复后的代码

### 1. 拉取最新代码

```bash
cd /kaggle/working/PRAG
git pull origin main  # 或您使用的分支
```

或者手动修改`src/inference.py`文件，应用上述修复。

### 2. 重新运行推理

```python
from inference import main as inference_main
import argparse

args_inference = argparse.Namespace(
    model_name='qwen2.5-1.5b-instruct',
    dataset='popqa',
    data_type='total',
    with_cot=False,
    augment_model='qwen2.5-1.5b-instruct',
    num_train_epochs=1,
    learning_rate=0.0003,
    lora_rank=2,
    lora_alpha=32,
    inference_method='combine',  # 先测试combine模式
    sample=10,  # 先用小样本测试
    max_new_tokens=20,
)

inference_main(args_inference)
```

### 3. 检查结果

修复后的预期结果：
- **ICL**: F1 ≈ 0.10（不变）
- **PRAG**: F1 ≈ 0.19（应该显著提升！）
- **COMBINE**: F1 ≈ 0.20+（应该比ICL好）

---

## 诊断工具

如果修复后仍有问题，使用诊断脚本：

```python
!python src/diagnostic_test.py \
    --model_name=qwen2.5-1.5b-instruct \
    --dataset=popqa \
    --num_train_epochs=1 \
    --learning_rate=0.0003 \
    --lora_rank=2 \
    --lora_alpha=32
```

诊断脚本会检查：
1. ✓ 数据加载是否正确
2. ✓ LoRA权重文件是否存在
3. ✓ 适配器能否正确加载和合并

---

## 其他可能的修复方案

如果上述修复仍不起作用，请查看`POTENTIAL_FIXES.md`文件，其中包含：
- Fix 2: 添加更多调试信息
- Fix 3: 升级PEFT版本到0.14.0+
- Fix 4: 不使用合并，单独使用每个适配器

---

## 重要参数对应

**编码和推理必须使用完全相同的参数**：

| 参数 | 说明 | 您的设置 |
|------|------|----------|
| `model_name` | 模型名称 | qwen2.5-1.5b-instruct |
| `dataset` | 数据集 | popqa |
| `data_type` | 数据类型 | total |
| `num_train_epochs` | 训练轮数 | 1 |
| `learning_rate` | 学习率 | 0.0003 |
| `lora_rank` | LoRA秩 | 2 |
| `lora_alpha` | LoRA alpha | 32 |
| `with_cot` | 是否使用CoT | False |
| `augment_model` | 增强模型 | qwen2.5-1.5b-instruct |

**LoRA权重保存路径**：
```
offline/qwen2.5-1.5b-instruct/rank=2_alpha=32/popqa/lr=0.0003_epoch=1_direct/aug_model=qwen2.5-1.5b-instruct/total/data_{did}/passage_{pid}/
```

---

## 检查清单

在运行推理前，确认：

- [ ] encode.py已成功运行，没有报错
- [ ] LoRA权重文件存在：`find offline/ -name "adapter_model.safetensors" | head`
- [ ] 文件大小不为0：`find offline/ -name "adapter_model.safetensors" -exec ls -lh {} \;`
- [ ] 数据包含augment字段：检查`data_aug/popqa/qwen2.5-1.5b-instruct/total.json`
- [ ] encode和inference参数完全匹配

---

## 需要进一步帮助？

如果问题仍然存在：

1. **运行诊断脚本**并分享输出
2. **检查LoRA文件**：
   ```bash
   find offline/ -name "adapter_model.safetensors" | wc -l
   # 应该有 300 samples × 3 passages = 900 个文件
   ```
3. **测试单个样本**：
   ```python
   args_inference.sample = 1  # 只测试1个样本
   ```
4. **查看GitHub Issues**，搜索类似问题

---

## 技术总结

### 问题本质
PEFT 0.13.2的`add_weighted_adapter`方法在使用`combination_type="cat"`时存在bug，导致合并后的适配器无法正常工作。

### 解决方案
改用`combination_type="linear"`并使用归一化权重，这样可以避开PEFT库的bug，让LoRA适配器正确合并并发挥作用。

### 期望效果
修复后，PRAG和COMBINE模式应该能正确使用LoRA权重，性能应该达到或超过论文报告的水平。

---

## 相关文档

- `TROUBLESHOOTING.md` - 完整的故障排除指南（英文）
- `POTENTIAL_FIXES.md` - 详细的修复方案和替代方案（英文）
- `src/diagnostic_test.py` - 诊断测试脚本

---

**祝您使用顺利！如有问题，欢迎在GitHub提Issue。**
