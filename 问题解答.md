# 问题解答

## ❓ 您的问题

> 那这个问题是什么原因呢？解决了吗
> 
> ```
> ### Solving total ###
>   0%|                                                    | 0/10 [00:00<?, ?it/s]
> Traceback (most recent call last):
>   File "/kaggle/working/PRAG/src/encode.py", line 241, in 
> ```

## 📋 问题原因

### 主要原因 (95% 可能性)

**数据路径不匹配导致的 FileNotFoundError**

您的错误是因为：

1. 您使用了模型名称 `qwen3-8b` 或 `qwen2.5-1.5b-instruct`
2. 但是对应的数据增强文件不在期望的路径

**具体说明**:
- 如果使用 `--model_name qwen3-8b`，程序会查找 `data_aug/2wikimultihopqa/qwen3-8b/total.json`
- 如果使用 `--model_name qwen2.5-1.5b-instruct`，程序会查找 `data_aug/2wikimultihopqa/qwen2.5-1.5b-instruct/total.json`
- 如果文件不存在，就会在 `src/utils.py` 第 81 行的 `os.listdir(input_dir)` 处抛出错误

### 错误发生位置

```python
# src/utils.py 第 78-81 行
def load_data(data_name, data_type, model_name):
    solve_dataset = []
    input_dir = os.path.join(DATA_ROOT_DIR, data_name, model_name)
    files = [f for f in os.listdir(input_dir)]  # ← 这里报错
```

如果 `input_dir` 不存在，就会报：
```
FileNotFoundError: [Errno 2] No such file or directory: 'data_aug/2wikimultihopqa/qwen3-8b'
```

## ✅ 是否已解决？

### 代码层面: 已修复 ✅

我们已经修复了以下问题：
1. ✅ 添加了 `qwen3-8b` 的正确模型映射
2. ✅ 恢复了 `qwen2.5-1.5b-instruct` 的正确路径
3. ✅ 修复了 `torch_dtype` 弃用警告

**提交记录**:
- `15abd5d` - Fix Qwen3-8B support and torch_dtype deprecation
- `88e8e33` - Fix Qwen3-8B model support and torch_dtype deprecation  
- `5362ff0` - Add comprehensive error diagnosis document

### 使用层面: 需要您操作 ⚠️

代码修复了，但您还需要：

#### 方案 A: 如果想使用 Qwen3-8B

```bash
# 步骤 1: 检查数据是否存在
ls data_aug/2wikimultihopqa/

# 步骤 2: 如果数据在 qwen2.5-1.5b-instruct/ 文件夹，重命名它
mv data_aug/2wikimultihopqa/qwen2.5-1.5b-instruct \
   data_aug/2wikimultihopqa/qwen3-8b

# 步骤 3: 运行训练，使用正确的模型名称
python src/encode.py \
    --model_name qwen3-8b \
    --dataset 2wikimultihopqa \
    --data_type total \
    --sample 10 \
    --num_train_epochs 1 \
    --learning_rate 3e-4 \
    --lora_rank 2 \
    --lora_alpha 32 \
    --with_cot
```

#### 方案 B: 如果数据是用 qwen2.5-1.5b-instruct 生成的

```bash
# 直接使用原来的模型名称
python src/encode.py \
    --model_name qwen2.5-1.5b-instruct \
    --dataset 2wikimultihopqa \
    --data_type total \
    --sample 10 \
    --num_train_epochs 1 \
    --learning_rate 3e-4 \
    --lora_rank 2 \
    --lora_alpha 32
```

#### 方案 C: 重新生成数据（如果没有数据）

```bash
# 先生成数据增强文件
python src/augment.py \
    --model_name qwen3-8b \
    --dataset 2wikimultihopqa \
    --data_path data/2wikimultihopqa/ \
    --sample 300 \
    --topk 3

# 然后运行训练
python src/encode.py \
    --model_name qwen3-8b \
    --dataset 2wikimultihopqa \
    --data_type total \
    --sample 10 \
    --num_train_epochs 1 \
    --learning_rate 3e-4 \
    --lora_rank 2 \
    --lora_alpha 32 \
    --with_cot
```

## �� 如何验证问题已解决

### 步骤 1: 检查数据路径

```bash
# 查看您的数据在哪里
ls -la data_aug/2wikimultihopqa/

# 应该看到类似：
# drwxr-xr-x 2 user user 4096 Jan 28 qwen3-8b/
# 或
# drwxr-xr-x 2 user user 4096 Jan 28 qwen2.5-1.5b-instruct/
```

### 步骤 2: 检查文件内容

```bash
# 查看数据文件是否存在
ls data_aug/2wikimultihopqa/YOUR_MODEL_NAME/

# 应该看到 total.json 或其他 .json 文件
```

### 步骤 3: 测试运行

```bash
# 用小样本测试（只处理 1 个数据）
python src/encode.py \
    --model_name YOUR_MODEL_NAME \
    --dataset 2wikimultihopqa \
    --data_type total \
    --sample 1 \
    --num_train_epochs 1 \
    --learning_rate 3e-4 \
    --lora_rank 2 \
    --lora_alpha 32

# 如果成功，您会看到：
# ### Solving total ###
# 100%|█████████████████████████████| 1/1 [XX:XX<00:00, XX.XXs/it]
```

## 📚 相关文档

详细信息请查看：

1. **`ERROR_DIAGNOSIS.md`** (提交 5362ff0)
   - 完整的错误诊断
   - 所有可能的原因
   - 详细的调试步骤

2. **`FIX_SUMMARY_CN.md`**
   - Qwen3-8B 支持修复总结
   - 使用示例

3. **`docs/Qwen3-8B支持说明.md`**
   - 详细的使用文档

## 🎯 快速检查清单

在运行 `encode.py` 之前，确认：

- [ ] 已经运行过 `augment.py` 生成数据
- [ ] 数据文件在正确的路径 (`data_aug/{dataset}/{model_name}/`)
- [ ] 使用的 `--model_name` 与数据路径中的文件夹名称一致
- [ ] 数据文件不为空且格式正确

## 💬 总结

**问题是什么？**  
→ 数据路径不匹配，程序找不到数据文件

**解决了吗？**  
→ 代码已修复 ✅，但您需要确保数据路径正确

**下一步做什么？**  
→ 按照上面的方案 A/B/C 之一操作，确保模型名称与数据路径匹配

---

**如果还有问题**，请提供完整的错误信息：

```bash
python src/encode.py [您的参数] 2>&1 | tee error_log.txt
```

然后查看 `error_log.txt` 文件的内容。
