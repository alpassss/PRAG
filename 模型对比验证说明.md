# 模型对比验证说明

## 目的

这个工具用于验证在inference时，LoRA适配器是否真的被加载并修改了模型参数。

**核心问题**：我们想知道inference时使用的是：
- ✓ 加载了LoRA权重的修改后模型
- ❌ 还是原始的基础模型

## 工作原理

### 验证流程

```
步骤1: 加载基础模型（不含LoRA）
         ↓
步骤2: 加载相同模型 + LoRA适配器
         ↓
步骤3: 比较两个模型的参数
         ↓
步骤4: 判断LoRA是否生效
```

### 判断标准

- **如果参数有差异** → LoRA适配器已激活 ✓
- **如果参数完全相同** → LoRA适配器未加载 ❌

## 使用方法

### 基本用法

```bash
python verify_lora_active.py \
    --model_name qwen2.5-1.5b-instruct \
    --adapter_path /kaggle/working/PRAG/offline/qwen2.5-1.5b-instruct/rank=2_alpha=32/complexwebquestions/lr=0.0003_epoch=1_direct/aug_model=qwen2.5-1.5b-instruct/total/data_0/passage_0
```

### 快速测试（只检查前3层）

```bash
python verify_lora_active.py \
    --model_name qwen2.5-1.5b-instruct \
    --adapter_path /kaggle/working/PRAG/offline/.../data_0/passage_0 \
    --sample_layers 3
```

### 参数说明

| 参数 | 必需 | 说明 | 示例 |
|------|------|------|------|
| `--model_name` | ✓ | 模型名称 | `qwen2.5-1.5b-instruct` |
| `--adapter_path` | ✓ | LoRA适配器完整路径 | `offline/.../data_0/passage_0` |
| `--sample_layers` | ✗ | 只检查前N层（可选） | `3` |

## 输出解读

### 成功案例（LoRA生效）

```
============================================================
步骤4: 验证结果
============================================================

总参数数量: 2048
改变的参数: 864
最大差异值: 0.523401

============================================================
✓ ✓ ✓ LoRA适配器已激活 - 模型参数发生了改变
============================================================

✅ 结论: LoRA权重在inference时正在生效
   864 个参数已被LoRA修改
   这证明加载的不是基础模型，而是LoRA修改后的模型
```

**解释**：
- 有864个参数发生了改变
- 最大差异值0.52说明改变是显著的
- ✅ LoRA正常工作

### 失败案例（LoRA未生效）

```
============================================================
步骤4: 验证结果
============================================================

总参数数量: 2048
改变的参数: 0
最大差异值: 0.000000

============================================================
❌ ❌ ❌ LoRA适配器未激活 - 所有参数都相同
============================================================

❌ 结论: LoRA权重可能没有正确加载
   可能的原因:
   1. adapter_path路径错误
   2. 适配器文件损坏
   3. 加载代码有问题
```

**解释**：
- 没有任何参数改变
- 差异值为0
- ❌ LoRA没有被加载

## 在Kaggle Notebook中使用

```python
# 1. 安装依赖
!pip install -q transformers peft torch

# 2. 复制脚本
!cp /kaggle/input/.../verify_lora_active.py /kaggle/working/

# 3. 运行验证
!python /kaggle/working/verify_lora_active.py \
    --model_name qwen2.5-1.5b-instruct \
    --adapter_path /kaggle/working/PRAG/offline/.../data_0/passage_0 \
    --sample_layers 3
```

## 常见问题

### Q1: 为什么要检查这个？

**A**: 确保inference时真的在使用LoRA权重，而不是基础模型。如果LoRA没有被加载，那么：
- PRAG模式 = 基础模型（无知识增强）
- COMBINE模式 = ICL模式（LoRA无效）

### Q2: 如果结果显示LoRA未激活怎么办？

**A**: 检查以下几点：
1. adapter_path是否正确（复制完整路径）
2. adapter_model.safetensors文件是否存在
3. inference.py中的加载代码是否正确（不要用adapter_name="0"）

### Q3: 改变多少个参数是正常的？

**A**: 取决于LoRA配置：
- rank=2, alpha=32：通常会改变几百到上千个参数
- 只要 `changed_params > 0`，就说明LoRA在工作
- 改变0个参数说明LoRA完全没加载

### Q4: 可以在inference.py中集成这个检查吗？

**A**: 可以！在inference.py加载完适配器后添加：

```python
# 加载适配器后
model = PeftModel.from_pretrained(model, adapter_path, is_trainable=False)

# 检查LoRA是否激活
lora_param_count = sum(1 for n, p in model.named_parameters() if 'lora' in n.lower())
print(f"LoRA参数数量: {lora_param_count}")
if lora_param_count > 0:
    print("✓ LoRA适配器已激活")
else:
    print("❌ 警告: 没有检测到LoRA参数!")
```

## 与inference_debug.py的区别

| 工具 | 用途 | 检查内容 |
|------|------|----------|
| **inference_debug.py** | 调试加载过程 | 路径、文件大小、加载状态 |
| **verify_lora_active.py** | 验证LoRA效果 | 参数是否真的改变了 |

**推荐使用顺序**：
1. 先用 `inference_debug.py` → 确认文件存在、能加载
2. 再用 `verify_lora_active.py` → 确认参数真的改变了

## 代码说明

### 关键函数

**load_base_model(model_name)**
- 作用：加载纯净的基础模型（不含任何LoRA）
- 返回：基础模型对象

**load_lora_model(model_name, adapter_path)**
- 作用：加载基础模型 + 指定的LoRA适配器
- 返回：带LoRA的模型对象

**compare_models(base_model, lora_model, sample_layers)**
- 作用：逐参数比较两个模型
- 检查：参数名称、形状、数值差异
- 返回：统计结果（总参数、改变数、最大差异）

**print_results(stats)**
- 作用：打印友好的结果报告
- 输出：✓/❌ 判断 + 详细统计

## 总结

这个工具**明确回答**：
- ✅ LoRA权重是否在inference时被使用
- ✅ 模型参数是否因LoRA而改变
- ✅ 测试时用的是哪个版本的模型

**如果显示LoRA未激活，说明inference代码有问题；如果显示LoRA已激活但F1仍不提升，说明训练不足（需要增加epoch）。**
