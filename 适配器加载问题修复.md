# 关键问题修复：适配器命名不匹配

## 问题诊断

您看到的警告信息揭示了真正的问题：

```
UserWarning: Found missing adapter keys while loading the checkpoint: 
['base_model.model.model.layers.0.mlp.gate_proj.lora_A.0.weight', ...]
```

这个警告说明**适配器根本没有被正确加载**！这就是为什么修改合并方式后结果没有改善的原因。

## 根本原因

### 编码阶段（encode.py）

```python
# encode.py line 104
model = PeftModel.from_pretrained(model, init_adapter_path, is_trainable=True)
# 没有指定 adapter_name
# 保存的参数名称: lora_A.weight, lora_B.weight (没有后缀)
```

### 推理阶段（之前的 inference.py）

```python
# inference.py line 82-89 (旧代码)
model = PeftModel.from_pretrained(model, adapter_path, adapter_name="0", ...)
# 指定了 adapter_name="0"
# 期望的参数名称: lora_A.0.weight, lora_B.0.weight (带 .0 后缀)
```

### 不匹配！

- **保存时**: `lora_A.weight`, `lora_B.weight`（无后缀）
- **加载时**: `lora_A.0.weight`, `lora_B.0.weight`（有后缀）

结果：PEFT找不到匹配的权重，适配器实际上是空的！这就是为什么：
- COMBINE模式 = ICL模式（LoRA没有效果）
- PRAG模式表现很差（完全依赖空的LoRA）

## 修复方案

### 已修改的代码

**src/inference.py** 中的关键修改：

```python
# 修复后：不指定 adapter_name，让PEFT使用默认名称
if pid == 0:
    model = PeftModel.from_pretrained(
        model, 
        adapter_path,
        is_trainable = False  # 不指定 adapter_name
    )
    # 获取自动分配的适配器名称
    first_adapter_name = model.active_adapter if hasattr(model, 'active_adapter') else "default"
else:
    # 为后续适配器使用唯一名称
    model.load_adapter(adapter_path, adapter_name=f"adapter_{pid}")

# 获取所有已加载适配器的名称
if hasattr(model, 'peft_config'):
    adapter_names = list(model.peft_config.keys())

# 使用实际的适配器名称进行合并
model.add_weighted_adapter(
    adapters = adapter_names[:num_adapters], 
    weights = [1.0 / num_adapters] * num_adapters,
    adapter_name = "merge", 
    combination_type = "linear",
)
```

## 关于您的两个问题

### 问题1: encode代码是否等同于 `python3 src/encode.py`？

**是的**，您notebook中的encode代码功能上等同于直接运行 `python3 src/encode.py`，都会：
1. 加载数据和模型
2. 为每个passage训练LoRA适配器
3. 保存到 `offline/` 目录

区别只是：
- 命令行方式：使用argparse解析参数
- Notebook方式：使用argparse.Namespace直接创建参数对象

两种方式生成的LoRA权重文件完全相同。

### 问题2: 警告是结果不好的根本原因吗？

**是的！** 这些警告不是小问题，而是**致命错误**：

1. **"Found missing adapter keys"警告**：
   - 说明适配器参数完全没有被加载
   - LoRA权重是空的或者使用的是随机初始化
   - 这就是为什么COMBINE = ICL（LoRA完全无效）

2. **其他警告**（generation配置）：
   - 这些是良性警告，不影响结果
   - 只是提示某些未使用的参数

## 如何验证修复

### 步骤1: 更新inference.py

拉取最新的修复代码或手动应用上述修改。

### 步骤2: 重新运行推理

```python
from inference import main as inference_main
import argparse

args_inference = argparse.Namespace(
    model_name='qwen2.5-1.5b-instruct',
    dataset='popqa',
    data_type='total',
    with_cot=False,
    augment_model='qwen2.5-1.5b-instruct',
    num_train_epochs=1,
    learning_rate=0.0003,
    lora_rank=2,
    lora_alpha=32,
    inference_method='combine',
    sample=10,  # 先用小样本测试
    max_new_tokens=20,
)

print("测试修复后的COMBINE模式...")
inference_main(args_inference)
```

### 步骤3: 检查警告

修复后，您应该**不再看到**"Found missing adapter keys"警告！

如果仍然看到这个警告，说明：
1. 代码没有正确更新
2. 或者LoRA权重文件本身有问题

### 步骤4: 检查结果

修复后的预期结果：
- **ICL**: F1 ≈ 0.10（不变）
- **PRAG**: F1 ≈ 0.19（从0.0287大幅提升）
- **COMBINE**: F1 ≈ 0.20+（从0.0999提升）

## 为什么之前的修复无效

之前我们修改了合并方式（从"cat"改为"linear"），但这个修复只有在适配器**正确加载**的情况下才有用。

由于适配器命名不匹配，适配器根本没有被加载，所以：
- 无论使用"cat"还是"linear"都没用
- 合并的是空适配器或随机权重
- COMBINE模式退化为ICL模式

现在的修复解决了根本问题：
1. ✅ 适配器可以正确加载（无警告）
2. ✅ 合并使用正确的方式（linear）
3. ✅ PRAG和COMBINE模式应该正常工作

## 技术细节

### PEFT的adapter_name机制

- **不指定adapter_name**: PEFT使用默认名称，参数名不带数字后缀
- **指定adapter_name**: PEFT在参数名后添加后缀（如`.0`，`.1`）

这是为了支持同时加载多个适配器而设计的机制。

### 为什么encode没有使用adapter_name

因为encode每次只训练一个适配器，不需要区分多个适配器，所以：
- 使用默认加载方式（无adapter_name）
- 保存的文件可以直接用于单适配器场景

### 为什么之前的inference使用了adapter_name

原代码可能是为了支持多适配器场景，但没有考虑到与encode的兼容性。

## 下一步

1. **更新代码**：拉取最新的inference.py
2. **运行测试**：使用小样本（sample=10）验证
3. **检查警告**：确认"Found missing adapter keys"消失
4. **完整推理**：如果测试通过，运行完整的300个样本

如果结果仍然不理想，可能需要：
- 检查LoRA权重文件是否完整
- 验证训练是否成功（损失是否下降）
- 考虑增加训练轮数（num_train_epochs=2）

但首先，适配器必须能够正确加载！
